#include <iostream>
#include <stdio.h>
#include <math.h>
    
using namespace std;

int main()
{
    int base, exp, result; // POTENCIA = guarda as potencias parciais, base = base, exp = Expoente

  cout << "\n\tCalculo de potencias\n";
  cout << "Digite um numero inteiro: "; cin >> base;

  cout << "Digite um numero um inteiro nao-negativo: "; cin >> exp;
  
 
 
  
  // Calculo da potencia
  // potencia = base * base;
  // mult = potencia * base;
    
  
        
        //mult = (potencia * base);
        //contador = contador + 1;
    

  cout << "O valor de: " <<base; cout << "\televado a: " <<exp; cout << "\té igual á: " <<result<<endl;

    
    return 0;
}
