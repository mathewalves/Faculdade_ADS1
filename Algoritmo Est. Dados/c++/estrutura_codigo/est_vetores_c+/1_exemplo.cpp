#include <iostream>

//Aula de Vetores.

using namespace std;

int main () {
    int tamanho;
    int vetor[tamanho];

    for(int i = 0;i < tamanho; i++){
            cout << "informe um numero:";
            cin >> vetor[i];
            cout << "\n";
    }
    for(int i = 0; i < 10; i++){
            cout << "\nO valor absoluto:";
        if(vetor) ([i] < 0);
        cout << vetor[i] * (-1);
        else
        cout << vetor [i];
    }

getch();
}

